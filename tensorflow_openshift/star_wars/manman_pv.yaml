apiVersion: v1
kind: Pod
metadata:
  name: manman-training
spec:
  restartPolicy: OnFailure
  containers:
    - name: manman-training
      image: 172.30.1.1:5000/mnist/manman-training:latest
      imagePullPolicy: Always
      volumeMounts:
      - mountPath: "/tf_files"
          name: "manman-pv"
  volumes:
    -
      name: "manman-pv"
      persistentVolumeClaim:
        claimName: "nfs-claim1"
user@f0:~/Desktop/ManMan/tensorflow_openshift/star_wars$ cat manman_pv.yaml 
apiVersion: v1
kind: PersistentVolume
metadata:
  name: manman-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: /tf_files
    server: 10.0.2.30

